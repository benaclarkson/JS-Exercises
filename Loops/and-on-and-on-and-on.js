/**
 * Task: The following code keeps looping forever. (You can hit Ctrl-C to stop it.) Why is that? Also modify it so that it stops after the first iteration.

 * Solution: The reason this `for` loop loops ininitely is because there is no condition listed as to when it should stop.
**/

for (let i = 0; i < 1; i += 1) {
  console.log("and on");
}