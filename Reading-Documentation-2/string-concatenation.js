/**
 * Task: Find out how we can join two or more strings together.

 * Solution: The `String.prototype.concat()` method will join two or more strings together.
 * We can also use a template literal or the `+` operator on more than one string to join them together
**/